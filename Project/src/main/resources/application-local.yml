spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/shop_db
    username: admin
    password: 3121
    driver-class-name: org.postgresql.Driver

  data:
    redis:
      host: localhost
      port: 6379
      database: 0

logging:
  level:
    org.example.project: DEBUG
    org.springframework: INFO